---
import Layout from '../../../layouts/Layout.astro';

const referralQuestions = [
	'Questions about dyslexia, reading fluency, or reading comprehension',
	'Need for a clear explanation of the learning profile and school implications',
	'Need for recommendations that are easy to use at home and in school settings',
];

const facts = [
	{ label: 'Format', value: 'Remote evaluation' },
	{ label: 'Reach', value: '43+ states via PSYPACT' },
	{ label: 'Turnaround', value: 'Evaluation and report within one week' },
	{ label: 'Rate', value: '$1,500 private-pay' },
];
---

<Layout
	title="Dyslexia Testing | Morning Tide Consulting and Collective"
	description="Remote dyslexia evaluations across 43+ PSYPACT states with a polished report typically completed within one week."
>
	<main class="px-6 py-16 lg:px-10 lg:py-20">
		<section class="mx-auto max-w-6xl space-y-8">
			<p class="eyebrow">Psychoeducational Evaluations</p>
			<h1 class="max-w-4xl text-5xl leading-none text-(--color-ink) sm:text-6xl">Dyslexia testing — remote, efficient, and tailored to your child.</h1>
			<p class="max-w-3xl text-lg leading-8 text-(--color-muted)">
				For families who want a clear dyslexia evaluation without a long wait. MCC provides remote access across 43+ PSYPACT states and typically completes the report within one week.
			</p>
		</section>

		<section class="mx-auto mt-12 grid max-w-6xl gap-6 lg:grid-cols-[1fr_0.95fr]">
			<div class="section-shell rounded-4xl p-8">
				<p class="eyebrow">Common Referral Questions</p>
				<h2 class="mt-4 text-4xl leading-none text-(--color-ink)">Fast answers for important reading questions.</h2>
				<ul class="mt-6 space-y-3 text-sm leading-7 text-(--color-muted)">
					{referralQuestions.map((item) => (
						<li class="flex gap-3">
							<span class="mt-2 h-2 w-2 shrink-0 rounded-full bg-(--color-brand)"></span>
							<span>{item}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="rounded-4xl border border-(--color-line-soft) bg-(--color-surface) p-8 shadow-(--shadow-soft)">
				<p class="eyebrow">At a Glance</p>
				<div class="mt-6 grid gap-4 sm:grid-cols-2">
					{facts.map((fact) => (
						<div class="premium-card rounded-3xl p-5">
							<p class="text-[0.68rem] font-semibold uppercase tracking-[0.28em] text-(--color-brand)">{fact.label}</p>
							<p class="mt-3 text-base font-semibold text-(--color-ink)">{fact.value}</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<section class="mx-auto mt-10 max-w-6xl rounded-4xl border border-(--color-line-soft) bg-(--color-surface) p-8 shadow-(--shadow-soft)">
			<div class="grid gap-8 lg:grid-cols-3">
				<div>
					<p class="eyebrow">Private-Pay Only</p>
					<p class="mt-4 text-base leading-8 text-(--color-muted)">MCC does not accept insurance for evaluations. The evaluation rate is $1,500.</p>
				</div>
				<div>
					<p class="eyebrow">Privacy</p>
					<p class="mt-4 text-base leading-8 text-(--color-muted)">Private-pay evaluations keep sensitive learning information private while keeping the process simpler and faster.</p>
				</div>
				<div>
					<p class="eyebrow">Next Step</p>
					<a class="mt-4 inline-flex rounded-full bg-(--color-accent) px-5 py-3 text-sm font-semibold text-white shadow-(--shadow-brand)" href="/#contact">Request dyslexia testing</a>
				</div>
			</div>
		</section>
	</main>
</Layout>
