---
import Layout from '../../../layouts/Layout.astro';

const referralQuestions = [
	'Questions about specific learning disorders and academic underperformance',
	'Need for recommendations that support accommodations, services, or school planning',
	'Need for a polished report on a fast and manageable timeline',
];

const facts = [
	{ label: 'Format', value: 'Remote evaluation' },
	{ label: 'Reach', value: '43+ states via PSYPACT' },
	{ label: 'Turnaround', value: 'Evaluation and report within one week' },
	{ label: 'Rate', value: '$1,500 private-pay' },
];
---

<Layout
	title="Learning Disorders | Morning Tide Consulting and Collective"
	description="Remote learning disorder evaluations across 43+ PSYPACT states with a polished report typically completed within one week."
>
	<main class="px-6 py-16 lg:px-10 lg:py-20">
		<section class="mx-auto max-w-6xl space-y-8">
			<p class="eyebrow">Psychoeducational Evaluations</p>
			<h1 class="max-w-4xl text-5xl leading-none text-(--color-ink) sm:text-6xl">Learning disorder evaluations that combine speed, clarity, and school-ready recommendations.</h1>
			<p class="max-w-3xl text-lg leading-8 text-(--color-muted)">
				MCC provides remote learning disorder evaluations across 43+ PSYPACT states for families who want a premium process, a manageable timeline, and a final report that is typically completed within one week.
			</p>
		</section>

		<section class="mx-auto mt-12 grid max-w-6xl gap-6 lg:grid-cols-[1fr_0.95fr]">
			<div class="section-shell rounded-[2rem] p-8">
				<p class="eyebrow">Common Referral Questions</p>
				<h2 class="mt-4 text-4xl leading-none text-(--color-ink)">Clear answers for educational planning without a long wait.</h2>
				<ul class="mt-6 space-y-3 text-sm leading-7 text-(--color-muted)">
					{referralQuestions.map((item) => (
						<li class="flex gap-3">
							<span class="mt-2 h-2 w-2 shrink-0 rounded-full bg-(--color-brand)"></span>
							<span>{item}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="rounded-[2rem] border border-(--color-line-soft) bg-(--color-surface) p-8 shadow-(--shadow-soft)">
				<p class="eyebrow">At a Glance</p>
				<div class="mt-6 grid gap-4 sm:grid-cols-2">
					{facts.map((fact) => (
						<div class="premium-card rounded-[1.5rem] p-5">
							<p class="text-[0.68rem] font-semibold uppercase tracking-[0.28em] text-(--color-brand)">{fact.label}</p>
							<p class="mt-3 text-base font-semibold text-(--color-ink)">{fact.value}</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<section class="mx-auto mt-10 max-w-6xl rounded-[2rem] border border-(--color-line-soft) bg-(--color-surface) p-8 shadow-(--shadow-soft)">
			<div class="grid gap-8 lg:grid-cols-3">
				<div>
					<p class="eyebrow">Private-Pay Only</p>
					<p class="mt-4 text-base leading-8 text-(--color-muted)">MCC does not accept insurance for evaluations. The flat evaluation rate is $1,500.</p>
				</div>
				<div>
					<p class="eyebrow">Privacy</p>
					<p class="mt-4 text-base leading-8 text-(--color-muted)">Remaining private-pay helps protect sensitive mental health and learning information while keeping the evaluation experience streamlined and discreet.</p>
				</div>
				<div>
					<p class="eyebrow">Next Step</p>
					<a class="mt-4 inline-flex rounded-full bg-(--color-accent) px-5 py-3 text-sm font-semibold text-white shadow-(--shadow-brand)" href="/#contact">Request a learning disorder evaluation</a>
				</div>
			</div>
		</section>
	</main>
</Layout>
